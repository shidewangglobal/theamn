<section id="about" class="tubik-section">
  <div class="tubik-shell">
    <div class="about-panel about-viewport bento-image relative overflow-hidden">
      <img
        src="/images/about-hot-tub-suite.png"
        alt="Luxury rooftop terrace with plunge pool at sunset"
        class="about-resort-img absolute inset-0 h-full w-full object-cover"
        loading="lazy"
      />

      <div class="about-glass-slot absolute inset-0 flex items-end p-3 sm:items-center sm:justify-end sm:p-5 md:p-7 lg:p-9">
        <div class="about-text-card about-glass-card about-glass-premium flex w-full max-w-[26rem] flex-col justify-center gap-5 px-6 py-7 sm:max-w-[min(88%,26rem)] md:max-w-[min(40%,26.5rem)] md:gap-6 md:px-8 md:py-9 lg:max-w-[38%] lg:px-10 lg:py-10">
          <h2 class="about-text-left font-headline text-[1.25rem] font-light leading-[1.32] tracking-[-0.02em] text-white sm:text-[1.4rem] md:text-[1.55rem] lg:text-[1.75rem]">
            <span class="about-phrase-nowrap">THE AMN is dedicated to elevating</span><br />
            <span class="headline-italic about-phrase-nowrap text-amn-blue">multi-market revenue</span><br />
            and <span class="headline-italic about-phrase-nowrap text-amn-blue">direct bookings</span><br />
            for premium hotels and resorts.
          </h2>
          <p class="about-text-right max-w-[34ch] font-body-secondary text-[0.9375rem] leading-[1.65] text-white/85 md:text-base lg:text-[1.0625rem]">
            We achieve this by harmonizing a
            <span class="text-accent-italic text-amn-lime">luxury-taste professional network</span>
            with
            <span class="text-accent-italic text-amn-lime">automated</span>,
            AI-driven digital marketing operations.
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about-viewport {
    min-height: min(72vh, 560px);
    max-height: min(82vh, 680px);
  }

  .about-phrase-nowrap {
    white-space: nowrap;
  }

  .about-glass-premium {
    background: rgba(8, 8, 10, 0.2);
    backdrop-filter: blur(24px) saturate(120%);
    -webkit-backdrop-filter: blur(24px) saturate(120%);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 1.5rem;
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.07),
      0 16px 48px rgba(0, 0, 0, 0.22);
  }

  .about-resort-img {
    object-position: center 45%;
    transform: scaleX(-1);
  }

  @media (max-width: 639px) {
    .about-viewport {
      min-height: 22rem;
      max-height: none;
    }
  }
</style>
